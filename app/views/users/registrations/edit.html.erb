<%= render "shared/header"%>
<div class ="m-3">
  <h2>アカウント編集</h2>

  <%= form_with model: @user, url: registration_path(resource_name), local: true do |f| %>
    <%= render "users/shared/error_messages", resource: resource %>

    <div class="mb-3">
      <%= f.label :email, class:"form-label"%><br />
      <%= f.email_field :email, autofocus: true, autocomplete: "email", class:"form-control w-25" %>
    </div>

    <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
      <div>現在、<%= resource.unconfirmed_email %>のメールアカウントの認証を待っている状態です。</div>
    <% end %>

    <div class="mb-3">
      <%= f.label :password, class:"form-label" %> <i>(変更したくない場合、空白にしてください)</i><br />
      <%= f.password_field :password, autocomplete: "new-password", class:"form-control w-25" %>
      <% if @minimum_password_length %>
        <div class ="form-text">最小文字数:<%= @minimum_password_length %>文字</div>
      <% end %>
    </div>

    <div class="mb-3">
      <%= f.label :password_confirmation,class:"form-label" %><br />
      <%= f.password_field :password_confirmation, autocomplete: "new-password", class:"form-control w-25" %>
    </div>

    <div class="mb-3">
      <%= f.label :name, class:"form-label" %>
      <%= f.text_field :name, autocomplete: "nickname", class:"form-control w-25" %>
    </div>

    <div class="mb-3">
      <%= f.label :current_password, class:"form-label" %>
      <%= f.password_field :current_password, autocomplete: "current-password", class:"form-control w-25" %>
      <div class="form-text">変更を認証するために、今のあなたのパスワードが必要です。</div>
    </div>

    <div class="actions">
      <%= f.submit "更新", class:"btn btn-primary" %>
    </div>
  <% end %>

  <div class="mt-4">
    <h3>アカウント削除</h3>
  </div>

  <p>よろしいですか? <%= button_to "アカウント削除", registration_path(resource_name), data: { confirm: "本当によろしいですか?",turbo_confirm: "本当によろしいですか?" },  method: :delete,class:"btn btn-primary" %></p>

  <%= link_to "戻る", :back %>
</div>